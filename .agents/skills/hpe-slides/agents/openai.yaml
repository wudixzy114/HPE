interface:
  display_name: "HPE Slides"
  short_description: "Create, verify, and release HPE decks and runtime shells"
  default_prompt: "Use $hpe-slides to build or audit an HPE deck, verify its rendered states, or create a runtime-only release without bundled presentations."
